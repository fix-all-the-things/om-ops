#!/usr/bin/env bash

set -ex

if [ $# != 1 ]; then
  echo "Usage: update_all TAG"
  echo "typically 'master' or 'staging' or '5-1-0' for tag"
  exit 1
fi

tag=$1

for i in *.nix; do
    test $i == default.nix && continue
    ./update_image $( basename $i .nix ) ${tag}
done
